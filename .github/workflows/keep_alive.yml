name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render /live
        run: |
          curl --fail --silent --show-error https://document-processing-agent.onrender.com/live
